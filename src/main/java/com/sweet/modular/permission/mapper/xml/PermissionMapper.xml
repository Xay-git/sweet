<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sweet.modular.permission.mapper.PermissionMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.sweet.modular.permission.entity.Permission">
        <id column="ID" property="id" />
        <result column="NAME" property="name" />
        <result column="URL" property="url" />
    </resultMap>
    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        ID, NAME, URL
    </sql>

    <select id="findByUserName" resultMap="BaseResultMap">
        select p.id,p.url,p.name from t_role r
        left join t_user_role ur on(r.id = ur.rid)
        left join t_user u on(u.id = ur.uid)
        left join t_role_permission rp on(rp.rid = r.id)
        left join t_permission p on(p.id = rp.pid )
        where u.user_name = #{userName}
    </select>

</mapper>
