<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sweet.modular.role.mapper.RoleMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.sweet.modular.role.entity.Role">
        <id column="ID" property="id" />
        <result column="NAME" property="name" />
        <result column="MEMO" property="memo" />
    </resultMap>
    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        ID, NAME, MEMO
    </sql>

    <select id="findByUserName" resultMap="BaseResultMap">
        select r.id,r.name,r.memo from t_role r
        left join t_user_role ur on(r.id = ur.rid)
        left join t_user u on(u.id = ur.uid)
        where u.user_name = #{userName}
    </select>

</mapper>
